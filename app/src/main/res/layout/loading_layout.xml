<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/lytLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

    <ImageView
            android:id="@+id/imgChuckLoader"
            android:layout_width="@dimen/imgChuckLoading"
            android:layout_height="@dimen/imgChuckLoading"
            android:visibility="visible"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
            android:id="@+id/txtLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/loading"
            android:textStyle="bold"
            android:textSize="@dimen/textSizeLoading"
            app:layout_constraintStart_toStartOf="@+id/imgChuckLoader"
            app:layout_constraintEnd_toEndOf="@+id/imgChuckLoader"
            app:layout_constraintTop_toBottomOf="@+id/imgChuckLoader" />

</android.support.constraint.ConstraintLayout>